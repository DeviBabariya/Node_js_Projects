<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookSaw - Edit Book</title>
    <style>
        /* Common Css */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: #F3F2EC;
        }

        .container {
            width: 1320px;
        }

        .d-flex {
            display: flex;
        }

        .row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        /* Header */

        .header {
            padding: 20px 10px;
            border-bottom: 1px solid #E0E0E0;
            background-color: #f3f2ec;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-logo img {
            margin-left: 10px;
        }

        .header-controls a {
            margin: 0 10px;
            text-decoration: none;
            font-size: 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #777777;
        }

        .header-controls a:hover {
            color: #0e0d0d;
            transition: all .1s ease-in-out;
        }

        .header-controls a.active {
            color: #daa556;
        }

        /* Form */

        h2 {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            font-size: 35px;
            margin-bottom: 10px;
        }

        .form-container {
            max-width: 600px;
            margin: 40px auto;
        }

        form {
            margin-top: 20px;
            background-color: #ffffff;
            padding: 30px 40px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 15px 20px;
        }

        td {
            font-size: 20px;
            color: #333;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 15px;
            font-family: inherit;
            transition: border-color 0.2s;
        }

        input[type="text"]:focus,
        input[type="number"]:focus {
            border-color: #daa556;
            outline: none;
        }

        input[type="submit"] {
            background-color: #daa556;
            color: white;
            border: none;
            padding: 10px 18px;
            width: 150px;
            margin-left: 10px;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
        }

        input[type="submit"]:hover {
            background-color: #b88440;
        }
    </style>
</head>

<body>

    <div class="header">
        <div class="row">
            <div class="header-logo" >
                <a href="/"><img src="https://themewagon.github.io/booksaw/images/main-logo.png" alt="logo"></a>
            </div>

            <div class="header-controls d-flex align-items-center ">
                <a href="/">Home</a>
                <a href="/add-book" style="margin-left: 15px;">Add Book</a>
            </div>
        </div>
    </div>

    <div align="center">
        <div class="form-container">
            <form action="/edit-book/<%= record.id %>" method="post">
                <h2>Edit Book Details</h2>
                <table>
                    <tr>
                        <td>Id:</td>
                        <td><input type="number" name="id" value="<%= record.id %>" disabled /></td>
                    </tr>
                    <tr>
                        <td>Title:</td>
                        <td><input type="text" name="title" value="<%= record.title %>" /></td>
                    </tr>
                    <tr>
                        <td>Author:</td>
                        <td><input type="text" name="author" value="<%= record.author %>" /></td>
                    </tr>
                    <tr>
                        <td>Genre:</td>
                        <td><input type="text" name="genre" value="<%= record.genre %>" /></td>
                    </tr>
                    <tr>
                        <td>Price:</td>
                        <td><input type="number" step="0.01" name="price" value="<%= record.price %>" /></td>
                    </tr>
                    <tr>
                        <td>Image URL:</td>
                        <td><input type="text" name="img" value="<%= record.img %>" /></td>
                    </tr>

                    <tr>
                        <td></td>
                        <td><input type="submit" value="Update Book" /></td>
                    </tr>
                </table>
            </form>
        </div>
    </div>
</body>
</html>